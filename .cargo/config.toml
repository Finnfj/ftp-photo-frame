# The target is deliberately not set here: cargo test, cargo clippy and rust-analyzer have to build
# for the host. Cross-compiling for the Raspberry Pi is done explicitly with `cross build --target
# aarch64-unknown-linux-gnu`, see BUILD.md.

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
